name: 🚀 Deploy GitHub Pages

on:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: 🔄 Sincronizar Dados
        run: node scripts/sync-data.js

      - name: 🔍 Validar Estrutura
        run: |
          test -f frontend/index.html || (echo "❌ index.html não encontrado" && exit 1)
          test -f frontend/data/clubes.json || (echo "❌ clubes.json não encontrado" && exit 1)
          echo "✅ Estrutura validada"

      - name: 📦 Upload para Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: "./frontend"

      - name: 🚀 Deploy
        id: deployment
        uses: actions/deploy-pages@v4

      - name: ✅ Sucesso
        run: |
          echo ""
          echo "🎉 DEPLOY CONCLUÍDO"
          echo "═══════════════════════════════════════"
          echo "🔗 URL: ${{ steps.deployment.outputs.page_url }}"
          echo "═══════════════════════════════════════"
